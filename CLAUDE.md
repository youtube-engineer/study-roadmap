# 参考書ロードマップ（プロダクト名は未確定）

参考書をどの順番で進めるかを、1本の経路として可視化・共有するWebアプリ。
このファイルは設計判断とその理由の記録。**「なぜそうしたか」を消さずに残すことが目的**なので、
方針を変えるときはこのファイルも更新すること。

---

## 1. 現在の状態

- **Next.js の本実装がある**（14章）。編集画面と共有ページが動く。
- **楽天ブックスAPIに接続済み**（2026年の新方式）。実データで動作を確認した。
- **保存はまだ無い。** ロードマップはプロセス内に置いているだけで、
  Supabase と IndexedDB は未着手。リロードすると戻る。
- `prototype/` は役目を終えたので削除した（React単体・esbuild）。
  検証済みの挙動は本実装に入っている。当時のコードはコミット `a1f89f2` に残っている。
- `docs/requirements.html` が要件定義書。ブラウザで開く。

---

## 2. 何を作るのか

参考書をどの順番でやるかを decide して、並べて、共有できるツール。

**やらないこと**（意識的に外している）:
- 学習時間の記録（Studyplusの領域。競合しない）
- 日次の進捗管理・何周目かのカウンター
- コメント機能、いいね機能（将来の検討対象、今はやらない）

**触る回数は少ない方がいい。** 計画を立てるときと、俯瞰するときにしか開かないアプリでいい。
これは妥協ではなく設計方針。毎日開かせる設計にすると学習記録アプリと同じ土俵に降りてしまう。

### 機能追加の判断基準（重要）

このアプリの競争優位は機能の多さではなく **目的が1つに絞られていること**。
既存サービスがこの領域に入れないのは、入ると自社プロダクトの中に別のメンタルモデルが
同居してしまうから（Studyplusは「学習時間の記録」が中核で、単位は1回のセッション。
ロードマップはセッションの上位にある構造物なので、タイムラインに置く器として形が合わない。
さらに収益が大学広告＝毎日開かれることに依存しているので、開く回数を減らす計画ツールは動機の面でも噛み合わない）。

**機能追加を検討するたびに「これはロードマップを主役のままにするか」を問う。**
- 講師による複数生徒の横断管理 → 生徒側の画面に何も足さないので許容できる
- コメント、詳細な進捗管理 → 生徒側の画面そのものを重くするので同じ扱いにはできない

脅威として現実的なのはStudyplusではなく、既にルート表示アプリ（ルートタケダ）を持つ武田塾、
または塾管理システム側（Comiru等）がカリキュラム管理に手を伸ばすケース。

---

## 3. 対象と焦点

**作れる内容はアプリ側で制限しない。** 制限は実装コストを払って可能性を狭める行為。
何のロードマップでも作れる状態にしたうえで、**発信と集客の焦点だけを英語・英検に置く**。
プロダクトの制約と、売り込む先は層を分けて考える。

英検を焦点にする理由:
- 級（3級→準2級→2級→準1級）が公式に定義された梯子になっている。
  「2級のルート」は誰にとっても同じ意味を持つので他人のロードマップをそのまま流用でき、共有の価値が上がる
- 合格すれば次の級のルートが必要になるので、受験生のように1年で完全離脱せず戻ってくる理由が構造に埋まっている
- 中学生から社会人まで同じ試験。将来の対象拡大と噛み合う
- 2024年度は高校生だけで133万人が受験、準1級は10年で約8倍

将来的には社会人の資格勉強・リスキリングまでが射程（市場が伸びている、本人が支払える、
1年で離脱しない）。ただし英語はTOEIC・ビジネス英語と地続きなので、教科の乗り換えではない。

---

## 4. 技術スタック

Next.js / TypeScript / Vercel / Supabase（DB・匿名認証） / shadcn/ui / **dnd-kit** / fractional-indexing

- **dnd-kit を採用**。@hello-pangea/dnd と Pragmatic DnD（Atlassian）と実際に3つ実装して比較し、
  最も自然でカクつきがなかった。react-beautiful-dnd は開発終了のため不採用
- 並び順は **fractional indexing**。1冊動かすたびに動かした行だけ更新する
- コードやテーブル名に**製品名を焼き込まない**（`roadmaps` / `roadmap_items` のような機能名で通す）。
  名前は未確定なので、後で改名コストが出ないようにする

---

## 5. 認証とデータ保存

- **ログイン必須にしない。** 使い始めるまでの摩擦をゼロにする
- 匿名認証（Supabase `signInAnonymously`）で自動バックアップ。ログイン時は `linkIdentity()` で
  昇格させる（データ移行が不要になる）
- IndexedDB に即時書き込み、Supabase へは裏で非同期同期。操作の体感速度はIndexedDBが担保する
- ログインはロードマップ完成時に促す（強制しない）

**匿名認証の「消える」は2種類あるので区別すること:**
- **ユーザーがアクセスを失う**（localStorageのトークン消失、端末変更）→ サーバー側の行は生きている。
  共有ページは `share_slug` で引くので表示され続ける
- **データが実際に消える** → 定期クリーンアップと、本人による明示的削除のみ

→ **公開済み（is_public=true）のロードマップはクリーンアップの対象外にする。**
他人がリンクを貼ったりコピー元にしている可能性があるため。

---

## 6. データモデル

```
roadmaps
  id
  owner_id            匿名/本アカウント共通のユーザーID
  title               自由入力
  is_public           共有リンクを有効にしているか
  share_slug          共有URL用の識別子
  copied_from_id      コピー元のID（自作ならnull）
  copied_from_title   コピー時点のスナップショット ※後述
  copied_from_name    コピー時点のスナップショット ※後述
  created_at

roadmap_items
  id
  roadmap_id
  book_id
  fractional_index    並び順（動かした行のみ更新）
  is_done             終えたか（1タップで切り替わる最小限の進捗信号）
  rounds_target       周回の「目標」（1〜20、未設定null）。進捗カウンターではない
  note                この本をどう使うかのメモ。共有時に相手が読む部分

books                 ← 「自分たちの書籍DB」ではなく「ISBNをキーにした表示用キャッシュ」
  id
  source              "rakuten" / "openbd" / "manual"
  external_id
  isbn                ← これだけが本当に自分のデータ（後述）
  title / author
  cover_image_url     URLだけ持つ。画像ファイルは複製しない
  source_url          出典ページ（Google由来なら表示必須だった。現状は楽天のみ）
  fetched_at          永続資産のつもりで持たないための目印

tags                  ユーザーが自由に作成
  id
  name                入力されたまま
  normalized_key      小文字化・空白除去・全角半角統一した突合用キー
  usage_count         補完候補の並び順と「◯人が使用」表示に使う

roadmap_tags          roadmaps ↔ tags の中間テーブル
```

### なぜ books は「キャッシュ」なのか

**ISBNだけが自分のもので、それ以外は借り物**という割り切り。
ISBNは国際標準の識別子で事実そのもの。「このロードマップの3番目にISBN xxx がある」は完全に自分のデータ。
一方で書名・著者・表紙は提供元の規約が付いてくる表示用データ。この2つを同じテーブル名で
ひとまとめにすると認識がずれる。

→ 提供元を切り替えるときはISBNから引き直す。**失っても致命傷にならない状態を保つ**。

**本当に価値がある資産は書誌データではない。** 「どの参考書が、どの順番で、どういうメモと一緒に
ルートに入れられたか」は100%自前のデータで、どのAPI提供元も権利を主張できない。
守るべきものと借りていて構わないものを取り違えない。

### タグを固定リストにしない理由

ユーザーがゼロの段階で正しいタグ体系を設計するのは、一人の推測を仕様に固定する行為。
自由に作らせて実際の語彙を観察する方が早い。表記の分散は閲覧する母数が育つまで発生せず、
問題になる頃には統合するためのデータが揃っている。

**ただし補完UIは必須機能。** 支援のない自由入力は単なる分散を生む。
Qiita同様、入力途中で既存タグを使用数付きで提示する（例:「簿記 1,203」）。
大半のユーザーは新規作成せず既存を選ぶので、表記揺れの吸収をルールではなくUIの誘導で行える。

**独立テーブル＋中間テーブルにするのはこのため。** 将来「日商簿記→簿記」の統合を行う際、
中間テーブルなら参照先IDの差し替えだけで済むが、`text[]` カラムだと全ロードマップの行を書き換えることになる。

タグは当面**任意**。コンテンツが充実してから共有時の必須項目に切り替える（移行を伴わない）。

### コピー（複製）の扱い

**参照ではなく実体の複製。** `roadmaps` と `roadmap_items` を新しい所有者のもとに作り直す。
元が書き換えられてもコピー側は影響を受けず、逆も同じ。

- 引き継ぐ: 並び順 / `book_id` / `rounds_target` / `note` / タイトル / タグ
- **引き継がない: `is_done`**（元の作成者の進捗であって自分のものではない）
- `copied_from_id` に元のIDを記録

**`copied_from_title` / `copied_from_name` をスナップショットとして持つ理由:**
元が削除されたり非公開に戻されるのは正当な行為なので、リンクは必ず切れる。
FKだけだと表示ごと消えてしまう。スナップショットがあれば
「◯◯さんのルートをもとにしています」の表示は残り、リンクだけが外れる形にできる。
**外部キーは辿るため、スナップショットは記録として残すため**と役割を分ける。

### RLS

- 本人の書き込み権限とは別に「`is_public=true` かつ `share_slug` 一致なら誰でも読み取り可」を
  **独立したポリシー**として用意する
- **アクセス制御はRLSに集約する。** 「本人のみ読める」判定をアプリ側に散らさない。
  散らばっていると閲覧できる主体が増えたとき修正箇所を追い切れなくなる
- 所有者は素直な外部キーで持つ。`roadmap_items` など下位テーブルへ非正規化して持ち回らない

（塾向けの横断管理は `organizations` / `memberships` を新規追加＋RLSポリシー追加だけで済み、
`roadmaps.owner_id` は生徒本人のまま変わらないので、**先回りした設計は不要**）

---

## 7. 外部APIと法務上の制約 ★実装前に必ず読む

### 採用: 楽天ブックス書籍検索API のみ

**Google Books API は使わない。** 利用規約 第1条 Fees に以下がある:

> "You may not charge users any fee for the use of your application, unless you have entered
> into a separate agreement with Google or obtained Google's written permission."

アプリ利用への課金が原則禁止。収益の主軸を塾向けサブスクに置くと正面から衝突する。

**Amazonアソシエイトも併用しない。** 楽天ウェブサービス規約の以下2条項に抵触する可能性が高い:
- **第8条4項**: 楽天サイトへのリンクを提供しなければならない。かつ**ウェブサービスが使用されている
  アプリケーションの部分において、楽天サイト以外へのリンクを提供してはいけない**
- **第10条1項(5)**: 楽天アフィリエイトの成果対象となる商品について、**楽天アフィリエイト以外の
  アフィリエイトプログラムを利用してはならない**（明示的許可がある場合を除く）

参考書は楽天ブックスで売られている＝楽天アフィリエイトの対象商品。**AmazonかRakutenの二択で両取り不可。**

### 楽天アフィリエイトでの収益化は正規の使い方

第3条がその場合の手順まで定めている。第10条1項(4)の禁止も「**楽天アフィリエイト以外の方法で**
収入を得ること」なので、楽天アフィリエイト経由なら想定内。無料B2C＋楽天アフィリエイトは規約上クリーン。

- **自己購入**: 違反ではないが成果にならない（確定処理で自動「破棄」）。踏んでも危険はない
- **親族・知人は明確に違反**。「生計を同一にする者」がリンク経由で成果を発生させる行為、
  示し合わせての購入は禁止事項。**予告なく利用停止の可能性あり**
- **クリックの呼びかけも禁止**（第10条1項(1)に「運営のためにクリックしてください」等が明示列挙）。
  アプリを勧めるのは問題ない。購入やクリックを呼びかけるのが問題

### アプリID登録の実務

- 楽天会員登録が必須。アプリ名・URL・応募タイプ・許可リスト・データ利用目的・想定QPSを入力すると即発行（無料）
- **応募タイプは「ウェブアプリケーション」を選ぶこと。**
  「API/バックエンドサービス」を選ぶと**IPアドレスでの許可リスト**になり、Vercelのサーバーレスは
  送信元IPが動的なので詰む。ウェブアプリケーションならドメイン指定になる
- 許可リストはワイルドカードを使えるが、**ドットで区切られた1区画まるごと**のみ。
  `*.example.com` は可、`*-foo.example.com` は不可（「valid list」エラーになる）
- APIスコープは **Rakuten Books だけ**。使うのは書籍検索APIのみ
- 鍵は **Next.js の Route Handler 経由でサーバーサイドからのみ**使う。
  ブラウザから直接叩くのは第5条2項（他者に知られないよう保管する義務）に触れる

### ★2026年の刷新（実装済み・実測で確認）

旧APIは2026年5月14日に停止。**旧方式のアプリIDは使えないのでアプリの登録し直しが必要。**

| | 旧 | 新 |
|---|---|---|
| ドメイン | `app.rakuten.co.jp/services/api/…` | **`openapi.rakuten.co.jp/services/api/…`** |
| 認証 | アプリIDだけ | **アプリID＋アクセスキーの両方**（片方だと400） |
| アプリID | 数字19桁 | **UUID形式** |
| アクセスキー | 無い | **`pk_…`**。`accessKey` ヘッダーで送る |

**7章の「未検証」だった疎通確認の答え:**

サーバーサイドの fetch は Origin を自動送信しないので、**自分で付けないと
403 `REQUEST_CONTEXT_BODY_HTTP_REFERRER_MISSING` が返る**。

→ **`Referer` では通らない。`Origin` でないと駄目**（エラー名に反する。実測で判明）。
値は許可リストに登録したドメイン。`RAKUTEN_ORIGIN` がその設定。

レート制限は**毎秒1回**。超えると429 `Rate limit is exceeded`。

### クレジット表記

**楽天**（必須）: 小バナー221×21px / 大バナー311×30px / テキスト「Supported by Rakuten Developers」の3種。
バナーは `webservice.rakuten.co.jp`、テキストは `developers.rakuten.com` へリンク。
**HTMLコード・画像はいずれも改変不可**。表示場所は**サイト/アプリ内のどこでもよい**と明記されている。

※ もしGoogle Books を将来併用するなら: Googleのガイドラインに
**「競合サービスのロゴとの並記は例外なく禁止」**がある。楽天ブックスは競合にあたるので
検索画面に両者のロゴを並べられない。楽天側が「どこでもよい」なので置き場所を分けることで解ける
（Powered by Google は検索シート、楽天は設定画面）。結果ごとの出典表示はロゴではなくテキストにする。

### 有料化するときは取得元を差し替える

無料B2Cの間は楽天でクリーン。ただし**塾向けサブスクを始めるなら、有料機能が楽天APIに依存しない形にするか、
取得元を切り替える**必要がある（第10条1項(4)）。

差し替え先の候補:
- **国立国会図書館サーチ**: キーワード検索可、法定納本ベースで網羅性最高、
  **営利利用は申請が必要**だが申請すれば可。料金徴収の禁止条項なし。書影は無い
- **openBD**: 無料・APIキー不要、日本語書誌と書影。制限は「本の販促・紹介目的に限る」という
  **目的の縛りで課金の禁止ではない**。ただし**ISBN指定のみでキーワード検索不可**（検索の主軸にはできない。
  ISBNが確定した後の補強用）
- **自前蓄積**: 参考書は領域が狭く、実際に使われる本は数千点で95%カバーできるはず。
  全書籍DBを作る話ではなく参考書マスタを育てる話

**→ 書誌データの取得経路を1箇所に抽象化しておくこと。** 「後で差し替える」を口約束にせず、
`books` への書き込み経路を1つに集約する設計として今やる。

### 依存リスクについての整理

APIが担うのは**入力の便利さであってプロダクトそのものではない**。
ロードマップの構造・順番・メモ・共有・コピーにAPIは一切関与しない。
全APIが止まっても既存ロードマップは生き、新規追加は手入力フォールバックで成立する。

またISBNという国際標準の識別子があるため、独自IDしか持たないAPIに比べて乗り換えコストが桁違いに低い。

依存を薄くする手:
- **バーコード読み取り**（APIを経由せずISBNが手に入る。ISBN→書誌はopenBDが無料・キー不要）
- **表紙に依存しない見た目**を用意しておく（書名の頭文字や色帯で代替）。
  書名や著者は事実だが**表紙は著作物**で、提供元ごとに条件が違う。リスクはここに集中している

---

## 8. 画面と操作

### 全画面を貫く規則（画面設計より）

1. **紐が構造を担う。** カードに枠線も影も置かない。始点・つながり・終点を紐と玉で
   表すことで、箱が等間隔に並んだ買い物リストではなく経路として読める
2. **色は2本だけ。** 朱（`--thread` `#e4572e`）は経路と進捗、青（`--accent` `#1b6cf2`）は操作。
   役割を混ぜない。琥珀（`--amber`）はメモの印にだけ使う。それ以外は紙と墨で、
   **本の表紙が唯一の彩り**。

   > 当初は臙脂と藍だったが、高校生に向けるにあたって彩度を上げた。
   > **2色という構造は変えていない。** 紫＋ピンクも試したが、参考書の表紙
   > （青・赤・黄が多い）とケンカして「本が主役」が弱まるのでやめた。
3. **見出しは太い角ゴ。** ルート名・段の名前・完走の見出しは太く大きく。
   明朝はサービス名（共有ページの上部）にだけ残す。
   高校生に向けるなら、紙の手触りより**読みやすさと元気さ**を採る
4. **握りは全高50px**（9章）
5. **破壊的な操作は一段奥へ。** 削除はカードではなくシートに置き、実行後は数秒だけ
   取り消せる帯を出す
6. **ロードマップを主役から降ろさない**（2章）

### 呼び方

利用者に見せる言葉は**「ロードマップ」**で統一する。

**「ルート」は使わない。** 武田塾（ルートタケダ）由来の言葉で、12章のとおり
「英語 参考書 ルート」の検索は新規ドメインで戦える相手ではない。使うと
「武田塾のルートを作れるアプリ」という位置に自分から収まる。

SEOの取りこぼしは心配しなくていい。12章の検索戦略は共有ページの長い裾で、
そこに載るのは**利用者がタイトルに書いた語**。UIの用語とは別物。

> このファイルの中では説明語として「ルート」を使っている箇所が残っているが、
> 画面に出る言葉は「ロードマップ」。

### 画面の幅

スマホは **430px**（画面設計）。**広い画面では広げる**——棚は横に並ぶので、
広がったぶんだけ本が多く見える。狭いままだと両脇が空くだけで、横スクロールも減らない。

| 幅 | 器 | 本 |
|---|---|---|
| 〜768px | 430px | 88×120 |
| 768px〜 | 720px | 112×153 |
| 1280px〜 | 920px | 128×175 |

本の大きさは CSS変数 `--book-w` / `--book-h` が1か所で決める。
`BookSpine` の `IMAGE_WIDTH/HEIGHT` は**画像の解像度を決めるためだけ**の値で、
見た目の大きさには関わらない（いちばん大きい場合に合わせてある）。

> フォントは仕様書では Shippori Mincho / Zen Kaku Gothic New を指定しているが、
> **実装ではシステムフォントを使う**（10章の初回表示）。

### 進捗の見せ方

**段ごとに棚板の上へ線を引く。** どこまで進んだかはここで読む。
朱は経路と進捗（規則2）なので役割も混ざらない。

ロードマップ全体の進捗は**出さない。** 段ごとの線と紐で足りるうえ、
上に置くと名前より先に数字が目に入る。

### 表紙の縦横比はそのまま使う

箱に合わせて `object-cover` で切ると、縦長の表紙は上下が削られる。
**本ごとに高さが変わるのは本棚として正しい見た目**でもある
（下端は `items-end` で棚板に揃う）。棚の高さはいちばん縦長の表紙に合わせる。

### ★横スクロールする棚の余白

本が側板に食い込んで見える問題を2回踏んだ。原因は別々だった。

- **余白は padding ではなく空要素で取る。** 横スクロールする flex の中では、
  左右どちらの `padding` もスクロールした位置で潰れる
- **吸着（`scroll-snap`）は使わない。** 本の左端を棚の左端に合わせにいくので、
  先頭の余白がぶんスクロールが勝手に進む（`scrollLeft` が 0 に戻らなくなる）。
  `scroll-padding` で逃げようとしたが効かなかった。棚では吸着の利点より害が大きい
- **側板の影は余白の幅ちょうどにする。** 広くすると、スクロールしていない
  状態でも端の本にかぶって暗くなる

### 一覧（左から出るドロワー）

**編集画面から直接開ける。** 一覧ページへ戻ってから開き直す形にしない。
ルートを行き来しながら組むので、切り替えに画面遷移を挟ませたくない。

`/` は一覧ではなく**最後に触ったルートを開く**。1本も無ければ空のルートを開く
（画面設計 01 の状態）。間に一覧を挟むと寄り道が1つ増えるだけになる。

**サーバーとローカルを突き合わせて出す。** 同じ id があればローカルを採り、
ローカルにしか無いもの——ログインする前に作ったもの——も並ぶ。
ネットワークを待たずに出せるのがこの層を持つ理由（10章）。

並び順は**最後に触った順**。作成日順だと、古いルートをずっと使っている人ほど
下に沈む。

**開いただけで何もしなかったルートは保存しない。** 一覧が空のルートで埋まると、
切り替える道具として使い物にならなくなる。

1人1本ではない。**複数持てることは、ログインを入れるなら必須**になる。
既にアカウントを持っている人が別端末で作りかけたものを持ち込むと、
持ち込んだ側と元々あった側の2本になるため（14章）。

### 走りきったとき

全部に印が付いた瞬間に出す。**誇りが最大化する一点**なので、共有とログインの両方をここに置く。

順序は意図的に**共有が上、ログインが下**。アカウントより先に、外に出ることを勧める。
共有が伸びるかどうかで全部が決まる（12章）。ログインは「機能を開ける鍵」ではなく
**「消えないようにする手段」**として出す。

一度出したら同じルートでは出さない。印を付け直すたびに祝われると鬱陶しい。

### 取り消し

**確認で手を止めさせず、まず実行して数秒だけ戻せる形にする。**

参考書を外したときも、ロードマップを消したときも同じ作りで、
**サーバーへの削除だけを遅らせる**。取り消されたら手元に戻すだけで済み、
消えたものを作り直す経路を持たなくていい。猶予のあいだに画面を離れたら、
その場でサーバーからも消す。

### 編集画面（棚）

**参考書は段（stage）でまとまり、段ごとの棚に立つ。**

段は「単語・文法」「長文」のようなまとまり。8章で取り下げた枝分かれ
（本線か補助かという性質の分岐）とは**別物**で、順番に進む区切り。経路は1本のまま。

- 左を**紐と玉が縦に通り、その右に棚が載る。** 紐が段と段をつなぐので、
  棚が並んだだけの画面にならない
- 棚は**木の箱として見せる。** 棚板（木目・木口・影）、奥板（内側の陰と側板）、
  本が触れているところの影の3つで奥行きを出す。
  画像は使わず全部CSSで作る（初回表示を遅らせないため。10章）
- **棚の内側に余白を取る。** 本が側板や棚板に食い込んで見えると、
  物として置かれている感じが消える
- 棚の中に置くものは**地の色ではなく光で見せる**（「＋追加」の枠など）。
  木の上では `--sunk` や `--rule` が沈んで見えなくなる
- 棚の上では**表紙がそのまま並ぶ。** 表紙が唯一の彩り（8章）なので、
  ここだけは画像を見せる。並ぶのが箱ではなく本なので、買い物リストには見えない
- 段の玉を押すと**段の中の本がまとめて切り替わる。** 段が終わったかどうかは
  持たない——中の本が全部終わっていれば終わり、と導出する。二重に持つと必ずずれる
- 本ごとの終了は表紙の右下に印が付く。終えた本は表紙が沈む
- 周回の目標とメモの印は棚板の上に出す（表紙を隠さない）
- 冊数は必ず出す。横スクロールで見切れている本に気づけるようにするため
- **棚は `overflow-y: hidden` を明示すること。** `overflow-x: auto` だけだと
  仕様上 `overflow-y` が `visible` から `auto` に変わり、中身が数pxでもはみ出して
  いると縦にも動いて本が上下にずれる。終了の印を表紙の内側に置いているのも同じ理由

#### ★棚の中はドラッグ、段をまたぐのはシート

棚は横スクロールする。**掴める範囲とスクロールは両立しない**（9章）ので、
9章と同じ形——**本の下端だけを握りにして `touch-action: none`、棚そのものは
`pan-x` のまま**——にしている。この2つはセットで意味を持つ。

握りは本の下端（表紙に接する26pxの帯）。**常に見えていること。**
ホバーで出す作りにしたらタッチ端末で事実上見えなくなった（ホバーが無いので）。
掴めることが分からなければ無いのと同じ。

**背景を敷かないこと。** 暗いグラデーションだと影に見えて本が浮き、明るい帯だと
表紙の下が白くなる。どちらも本そのものの見た目を変えてしまう。
点だけを置き、どんな表紙の上でも読めるように白い点へ暗い縁を付ける。
落ちる影（`--elev-book`）も最小限にする。

終了の印は握りと重なるので表紙の右上に置く。

**いったん握りから掴めば方向の制限は無い。** 掴む前のジェスチャーが
`pan-x` に取られるだけなので、握り（`none`）から始まった操作は縦にも横にも動く。

- **同じ段の中** … 本の握りをつまむ
- **段をまたぐ** … 同じく本の握り。別の棚へ持っていける
- **段の上下** … 段の玉（①②）をつまむ

**1つの DndContext が段と参考書の両方を見る。** 別々にすると、段をまたいで
本を動かすときに「どの棚へ落ちたか」が分からなくなる。空の棚にも落とせるよう、
棚そのものを droppable にしてある。

シートには「別の段へ移す」だけ残す。棚の中の並べ替えは握りでできるので、
同じことをするボタンを重ねて置かない。

棚の中の並べ替えボタンはシートに置かない。握りで同じことができるので、
重ねて置くと操作が2つに割れる。

**本を `<button>` にしないこと。** センサーはボタンの上でドラッグを始めない
作りにしてある（玉や操作ボタンの誤爆を防ぐため）ので、ボタンにすると握りが効かない。
`role="button"` を付けた div にする。

### 共有ページ### 共有ページ

読み取り専用。SSRで配信し検索エンジン経由の流入も狙う。

**編集画面の読み取り専用コピーにはしない。メモを開かずに読める形にする** ——
他人のルートを見る価値はまさにそのメモにあるので、**棚には立てず縦に積んで**
本文として展開する。段の見出しは残すので、どういう区切りで組まれたかは伝わる。
番号の点は数字のまま（終了の印は出さない。他人の進捗ではなく計画として読ませる）。

下端に「このルートをコピーして使う」。併せて「まっさらから作る」も控えめに置く。

共有シートには X・LINE・端末の共有メニューを並べる。**Instagram にはURLで投稿を開く
仕組みが無い**ので、端末の共有メニュー（`navigator.share`）から渡す。そこに Instagram が並ぶ。

> 楽天ウェブサービス規約 第8条4項の「楽天以外へのリンクを置かない」は、ウェブサービスを
> 使っている部分——検索シート——にかかる。共有シートは書誌データを出していないので対象外。

### 枝分かれ（補助教材）は採用しない

当初は「メインの背骨＋任意の枝分かれ」を採用したが、プロトタイプで触った結果**取り下げた**。
「ごく稀に挟みたい教材がある」という理由で入れたが、稀にしか使わない機能のために
全カードが「メインか補助か」の概念を背負うのは割に合わない。稀な教材は本線に入れれば順序で伝わる。

---

## 9. 実装時の落とし穴 ★これを知らないと必ず踏む

### touch-action: 掴める範囲とスクロールは両立しない

「カード全体を長押しで掴めるように」を実装したところ、実機で**掴めても動かせない**現象が出た。
ブラウザは**タッチ開始の時点で**そのジェスチャーに許す動作を確定するため、
縦スクロールを許可した要素の上ではドラッグが成立しない。

| touch-action | カード全体を長押しで掴む | 握りから即掴む | カード上でスクロール |
|---|---|---|---|
| `pan-y` | 不可 | 可 | 可 |
| `manipulation` | 不可 | 可 | 可 |
| `none` | 可 | 可 | **不可** |

ドラッグ開始後に動的に切り替える方法も試したが、進行中のジェスチャーには反映されず無効。

**採用する形**: カード本体は `pan-y` のままスクロールを通し、
**右端の握りだけを `touch-action: none`** にして即座にドラッグへ入れる。
握りが小さいと指で取れないので幅と高さを十分に確保する（幅50px・カード全高）。

長押し時のテキスト選択とiOSのコピーメニューを止めるため、操作領域に
`user-select: none` と `-webkit-touch-callout: none` を当てる（入力欄は除外）。
**`-webkit-touch-callout` はChromiumが未実装なのでiOS実機での確認が別途必要。**

### dnd-kit: 同じイベント名のセンサーを2つ登録すると無言で壊れる

「握りは即座に、カード本体は長押しで」を2つのセンサーで実現しようとすると**片方が効かなくなる**。
dnd-kitは同じイベント名（`onPointerDown`）を持つセンサーを、イベント名をキーにした
オブジェクトへ畳み込むため、後から登録した方が前を上書きする。

→ **センサーは1つにまとめ、押された位置を見て発火条件を切り替える。**
実装は `prototype/src/App.jsx` の `RoadmapSensor` を参照。

### hello-pangea/dnd と Pragmatic DnD: ボタンからドラッグを開始できない

比較検証時に踏んだ。ハンドルを `<button>` で作ると**無反応**になる（`<div role="button">` 相当にする）。
dnd-kit採用のため直接の影響はないが、ライブラリを変える場合は注意。

### 楽天ブックスAPIは書籍用と雑誌用が別

雑誌検索APIは `jan` と `booksGenreId=007（雑誌）`。書籍検索APIは `isbn` と `booksGenreId=001（本）`。
**参考書を探すなら書籍検索API（`BooksBook/Search`）**。

- `001002` = 語学・学習参考書
- 検索条件は title / author / publisherName / isbn / size / booksGenreId のいずれか1つ以上が必須
- **検索ではジャンルで絞らない。** ユーザーは書名で探すので、ジャンル固定は
  取りこぼしのリスクだけ増える。ジャンルIDを使うのは「何も打っていないときに
  何を並べるか」を決めるときだけ（実装済み）
- **BooksにランキングAPIは無い。** ジャンルを絞って `sort=sales` で代用する。
  IchibaのランキングAPIは別スコープが要るうえ、返るのが書誌ではなく商品なので使わない
- 売れ筋を出すジャンルは **`001002005`（語学関係資格）**。英検・TOEICの定番が並ぶ。
  親の `001002` や兄弟の `001002006`（学習参考書・問題集）だと、図鑑・ドリル・
  大学の赤本が上位を占めてランキングに見えない（実測）。焦点を英語・英検に
  置いている（3章）のとも合っている

---

## 10. 非機能要件

- **初回表示（LCP）**: 目標1.5秒、2.5秒はどんな環境でも超えない下限。
  画像・動画をほぼ持たないアプリなので一般的な2.5秒基準は緩すぎる。
  初回訪問者が見る画面は表示前のデータ取得が不要でエッジから静的配信でき、
  再訪問者の一覧もIndexedDBから読むためネットワーク往復を挟まない
  - dnd-kit と Supabase クライアントSDKは初回表示に不要なので `next/dynamic` で初期バンドルから外す
  - 日本語Webフォントは初回表示を遅らせやすい。本文はシステムフォント中心にする
- **検索タイムアウト**: 5秒でエラー表示（「該当なし」とは別の表現）に切り替える
- **表紙画像**: 画面内に入ってから読み込む遅延読み込み前提。画面内の1〜2冊のみ `priority`
- **対応ブラウザ**: iOS Safari / Android Chrome の最新版のみ。後方互換は追わない

---

## 11. 収益設計

**プロダクトは1つ。個人利用は無料、商用利用（塾・予備校・家庭教師）はサブスク契約。**
別プロダクトを作るのではなく、同じアプリに商用利用のライセンス層を重ねる。

**有料になる境界**は「複数の生徒を紐づけて横断的に見られること」。
個人利用者には発生しない要件なので、機能を意図的に削って人質にする必要がない。

### 単価の根拠

- **アフィリエイト（副次）**: Amazonの書籍紹介料率3.00%。1,500円の参考書で45円。
  ロードマップは一度組んだら終わりなので**1ユーザーあたり100〜200円の実質一回きり**。
  年100万円には1万人以上の購入者が必要で、**単独では開発時間に見合わない**
- **塾向けサブスク（主）**: 塾管理システムの相場は生徒数課金で月100〜200円/名、
  教室課金で月4,000〜24,000円/教室（ただし請求・出欠・保護者連絡まで含む総合システムの価格）。
  参考書ルートに特化したツールなら**月3,000〜6,000円/教室**が現実的

年間180万円に必要な規模: アフィリエイトなら約15,000〜20,000人が購入 / 塾向けなら**30教室**
（法人学習塾18,850事業所の約0.16%）。桁が違う。

### 学生向けアプリの位置づけ

**収益源ではなく認知・営業チャネル。** 生徒が個人で使っている状態が塾への導入経路になる。
「無料であること」「触る回数が少なくて済むこと」は収益とのトレードオフではなく、
この戦略において正しい設計。

順序: ①学生向け無料アプリでの認知 → ②個人利用の定着 → ③塾での商用利用が発生した時点でサブスク契約。
**①が動かない限り②③は始まらないので、当面の優先度は認知に置く。**

### 市場の前提

学習塾市場は約9,850億円で横ばい、法人の学習塾は全国に約18,850事業所（塾ポータル掲載では約11万教室）。
ただし少子化で通塾対象人口は過去最少（小学生581万人・中学生310万人＝ピーク時の4〜5割）、
赤字企業の割合は26.0%→31.8%へ上昇し二極化が進行。
**売る相手は「すべての塾」ではなく差別化に投資している側の塾。**

大学広告モデル（Studyplusの主収益）は会員495万人・高3の3人に1人という規模で初めて成立している。
**今回の射程には入れない。**

### 商用提供に伴う責任

塾を顧客にすると未成年の個人情報を業務として預かることになる。加えて請求・問い合わせ対応・
導入時の説明が発生し、これはコードを書くのとは別種の負荷。①②の段階では発生しないが、
③に進む判断をする時点で改めて引き受け可否を確認する。

---

## 12. 集客

**認知がすべての前提。** 予算ゼロ・フォロワーゼロから始まる。

- **主軸はX。** フィードバックが速く、安いコストで切り口を何度も試せる。
  YouTubeは制作コストが高く結果が出るまでも遅いので、当たりが見つかってから
- **発信内容は「自分の勉強」ではなく「アプリが持っている情報」。**
  日々の勉強記録は関係性がないと反応が来ない（相互フォロー・いいね回りの世界）。
  一方で実用的なtips・情報系の投稿は関係性がなくても拡散される
- **画像はほぼ必須。** Xのアルゴリズムがメディア付きを優遇し、勉強垢の文化自体が
  視覚的コンテンツ（ノート・文房具）中心で文字は補足という位置づけ
- **頻度は1日1投稿を継続が最低ライン。** 3日空くと非アクティブ判定のリスク。
  量より継続性（連投で露出を稼ぐ手法は今は逆効果）
- **SEO記事は分が悪い。** 「英語 参考書 ルート」の上位は武田塾・逆転合格.com・アクシブアカデミー・
  マナビズム・現論会と全部塾。新規ドメインで正面から戦う相手ではない。
  記事を書くならnote等でXからの受け皿として使う
- **本命の検索対策は共有ページ。** ユーザーが作ったロードマップの公開URL1つ1つが
  「特定の参考書ルートについてのページ」になる。ただしユーザーがいないと成立しない後半の打ち手

**最優先の検証: 誰かが頼まれてもいないのに自分のロードマップを共有するか。**
これが動けばSNS戦略もUGC戦略もSEO戦略も生きる。動かなければ下流の設計は全部前提から崩れる。

---

## 13. 未確定事項

- **共有とログインの関係**: ログインを「共有の条件」にすると成長エンジンを自分で絞ることになるので、
  ハードな必須にはしない案が有力。代わりに**「名前が載る条件」にする**（匿名でも共有できるが、
  作成者名を出すにはログインが必要）。広く共有したい人ほど名前を出したいので動機の向きが合う
- **並び替えの取り消し**。削除は戻せるようにしたが、ドラッグの事故はまだ戻せない
- **イベントログで記録する項目**。※どの参考書が選ばれどこで離脱したかは
  **後から遡って取得できない唯一の項目**なので、初回リリース時点から記録を始めること
- **タグを入れるかどうか、入れるならいつか。** 画面からは一度外した
  （押しても何も起きないボタンを置いておく方が悪い）。6章の設計と
  `tags` / `roadmap_tags` は残してあるので、戻すのは補完UIを作るだけ
- **商用プランの価格・課金単位**（個人利用が定着してから）
- **プロダクト名**（`参考書ロードマップ` は説明語として使用中）
- **未成年の場合の楽天会員登録・アフィリエイト参加条件**の確認

---

## 14. コードの構造

```
src/
  app/
    layout.tsx              メタデータ・テーマカラー・430px の器
    page.tsx                入口。最後に触ったルートへ送る
    roadmaps/[id]/page.tsx  編集画面（一覧はここから開くドロワー）
    r/[slug]/page.tsx       共有ページ。SSR。dnd-kitもシートも載らない
    not-found.tsx           元が削除・非公開になったとき
    api/books/search/       検索のRoute Handler。楽天を叩くのはサーバー側だけ
    api/books/persist/      books への書き込み。secret key を使う唯一の経路
  proxy.ts                  Supabaseのセッション更新（Next 16 は middleware ではなくこれ）
  components/
    roadmap/                経路の描画
    sheets/                 下から出るシート（検索・詳細・共有）
    ui/                     表紙・アイコン・トースト
  lib/
    books/                  ★書誌データの取得経路。差し替えるのはここだけ
    db/local.ts             ★IndexedDB。**保存の主役**
    dnd/roadmap-sensor.ts   ★実機検証済みのセンサー
    roadmaps/               store（読み）/ sync（Supabaseへ送る）/ copy（複製）
    supabase/               client（ブラウザ）/ server / admin（secret key）
  types/roadmap.ts          カラム名は6章のデータモデルに合わせてある
supabase/migrations/        スキーマ・RLS・GRANT。preview と本番の両方に流す
```

### 保存はローカルが主、Supabaseが従

5章の設計どおり、**IndexedDB が主で Supabase が従**。ここを取り違えないこと。

- **`lib/db/local.ts`** … 操作のたびに即座に書く。ここに書けていれば操作は失われない。
  Supabase の鍵が無くてもアプリは完成した状態で動く
- **`lib/roadmaps/sync.ts`** … その裏で Supabase へ送るだけ。失敗しても画面は巻き戻さない
- **`lib/roadmaps/store.ts`** … 読み。サーバーコンポーネントから。
  どの行が見えるかはRLSが決めるので、ここに「本人かどうか」の判定を書かない

**ロードマップは丸ごと書き換える。** 1件が数KBしかないので、差分更新にすると
「どこまで書けたか」の状態が増えて壊れ方が読めなくなるだけ。差分が要るのは Supabase 側だけ。

### ルートの名前

見出しをそのまま打ち替える形にした。編集ボタンも編集モードも置かない。
名前を変えるのは多くて数回なので、動線を増やす方が邪魔になる。

- 打鍵のたびに IndexedDB へ即時保存。**Supabase へ送るのは入力欄から離れたときだけ**
  （1文字ごとに update を投げても意味がない）
- 空のまま離れたら「新しいルート」に戻す。共有ページの見出しとOGPが名無しになるため
- Enter は改行させず確定して閉じる。名前は1行

### 削除の取り消し

**サーバーへの削除だけを数秒遅らせている**（`RoadmapList`）。手元からは即座に消して
一覧から外すが、サーバーには猶予のあいだ何も起きない。

取り消されたら手元に書き戻すだけで済み、**消えたものを作り直す経路を持たなくていい**。
先にサーバーから消すと、復元のために「行を作り直して book_id を貼り直す」処理が要る
（`carry.ts` と同じ手間）。破壊的操作の復元にそんな経路を増やしたくない。

猶予のあいだに画面を離れたら、その場でサーバーからも消す。放置すると
「手元には無いがサーバーにある」状態が残るため。

13章の「取り消し」はロードマップの削除についてはこれで解決。**参考書をルートから
外したときはまだ取り消せない**（同じ作りにできるはず）。

### 環境変数は「空文字」で存在しうる

`??` は null / undefined のときしか代替に切り替わらない。Vercel では
**名前だけ作られて値が空**という状態が普通に起きる（連携が作る変数、
手で追加して値を入れ忘れた変数）。`??` で繋ぐと空の鍵をそのまま送ってしまい、
Supabase が `Invalid API key`（401）を返す。実際にこれで半日溶けた。

`lib/supabase/config.ts` の `firstNonEmpty` を通すこと。

### OAuthのコールバックはCookieを自分でレスポンスに載せる

`next/headers` の `cookies()` に書いても、**Route Handler 内で自分が作った
`NextResponse` には乗らない。** 乗らないままリダイレクトすると、交換は成功して
いるのにセッションが残らない——「ログインできない」の形で現れる。

交換中に渡されるCookieを溜めておき、返すレスポンスへ明示的に `set` すること
（`app/auth/callback/route.ts`）。

### ログイン済みかどうかを is_anonymous で判定しない

`linkIdentity()` で昇格しても、**手元にあるトークンは発行時のまま
`is_anonymous: true` を主張し続ける**（更新されるまで古い claim が残る）。
それで判定すると、ログインしてもボタンが「ログイン」のまま残る。

紐づいた identity があるかどうかで見ること。匿名ユーザーは identity を持たない。

### ルートを切り替えたら編集画面を作り直す

`/roadmaps/[id]` で id だけが変わるとき、React は同じコンポーネントを使い回す。
`useState` の初期値は更新されないので、**前のルートの内容が残る**。
新しく作ったのに他人のルートの表示が出る、という形で現れた。

ページ側で `key={id}` を付けて作り直させている。状態が増えるほど効く対処なので、
編集画面に状態を足すときもこれを外さないこと。

### IndexedDB に保存する形を変えたらバージョンを上げる

`lib/db/local.ts` の `DB_VERSION` を上げて、`upgrade` に移行を書くこと。

**忘れると既存の利用者だけが壊れる。** 手元では新しいDBが作られるので気付かない。
実際 `Roadmap` に `createdAt` を足したとき、それ以前に保存されたレコードで
一覧の並べ替えが `undefined` を触って落ちた。

IndexedDB は各ブラウザに残り続ける。サーバーのマイグレーションと違って
「全員を一度に移す」ことができないので、**古い形が後から出てくる前提**で書く。

### 起動時にローカルとサーバーのどちらを採るか

**ローカルを無条件に勝たせてはいけない。** 一度そう実装して、共有ページの
「コピーして使う」が効かなくなった。サーバーに新しい文書ができて戻ってきても、
ローカルの前のロードマップが上書きしてしまうため。

そこでサーバーが渡すものを2種類に分けている（`store.ts` の `RoadmapSource`）。

| サーバー側 | ローカル | 採るもの |
|---|---|---|
| `placeholder`（まだ何も無い） | あり | **ローカル** |
| `stored` かつ id が一致 | あり | **ローカル**（同じ文書。ローカルの方が新しい） |
| `stored` かつ id が違う | あり | **サーバー**（別の文書＝コピー直後・まっさらから作る） |
| なんでも | 無し | サーバー |

`placeholder` は「サーバーには何も無い」という意味しか持たないので、
ローカルを消す根拠にならない。id が毎回変わるため、これを `stored` として扱うと
リロードのたびにローカルが飛ぶ。

**「まっさらから作る」は押した時点で実体を作る**（`copy.ts` の `startBlankForCurrentUser`）。
作らずに済ませると id が変わらず、ローカルに残っている前のロードマップが出てしまう。
訪問しただけではなく明確な意思表示なので、ここで匿名サインインしてよい。

> ⚠ **端末をまたいだときの突き合わせはまだ決めていない。**
> 上の表は「別の文書ならサーバー」という単純な規則で、更新時刻を見ていない。
> 別端末で編集したものがある状態でログインしたときにどちらを採るかは、
> ログインを実装する前に決めること。

### ログイン（Google）

**匿名ユーザーは `linkIdentity()` で昇格させる**（5章）。ユーザーIDが変わらないので、
それまでに作ったロードマップがそのまま自分のものとして残る。移行コードが要らない。

ただし**そのGoogleアカウントが既に別のユーザーに紐づいていると必ず失敗する**。
PCでログイン済みの人がスマホから、機種変更した人が新端末から、ストレージを消した人が
ログインし直すとき——どれもこれに当たる。ここを「失敗して終わり」にすると、
**既存アカウントに二度と入れなくなる**。バックアップのために促しているのに復元経路が無い、
という状態になるので必ず逃げ道を用意する。

逃げ道は `signInWithOAuth()` への切り替え。**説明は最初の一度だけ出す。**

何が起きるか（既存の記録を開き、手元のぶんは一緒に持っていく）を一度伝えれば、
二度目以降は同じ結果になると分かっている。毎回止めると、新しい端末を使うたびに
同じ確認を読まされることになる。しかも持ち込みが冪等になった時点で、
**二度目以降は押しても何も起きない確認**になっていた。

見たことを覚えるのは `localStorage`。`sessionStorage` だとタブを閉じるたびに
初回へ戻り、「毎回同じ確認が出る」状態に逆戻りする。

> 同じ端末では一度ログインすれば匿名に戻らないので、そもそも二度と出ない。
> 出るのは**新しい端末での初回ログイン**（機種変更、スマホで初めて開く、
> ストレージを消した後）。そこでも聞く価値は薄い。

```
昇格を試す（linkIdentity）
  └ 成功              → そのまま。データは動かない
  └ 「既に紐づいている」→ ?login=taken で戻る → 確認を出す
        └ 「そちらの記録を開く」→ signInWithOAuth で切り替え、
                                手元のぶんを新しい所有者のもとに作り直す
        └ 「やめる」          → 匿名のまま続ける
```

持ち込みは `lib/roadmaps/carry.ts`。**合体ではない。** 元々アカウントにあったものには
触れず、持ち込んだぶんが別のロードマップとして増えるだけ。どちらも消えない。
やっていることは `copy.ts` と同じで、違うのは元がサーバーではなく IndexedDB という点だけ。

> ⚠ **持ち込みは何度呼ばれても増えないこと。** IndexedDB は消えずに残り続けるので、
> ログインのたびにここを通る。「サーバーに自分のものとして既にあるか」を見ずに入れると、
> **ログインするたびに同じルートが増える**。実際そうなった。
> 所有者まで見るのは、公開されている他人のロードマップが RLS 越しに見えるため
> （見える ≠ 自分のもの）。

**この方式が成り立つのは一覧があるから。** 1人1本の画面のままだと、
持ち込んだ側だけが表示されて元々あった方が見えなくなる。

### 匿名サインインを打つ場所

**訪問しただけでは打たない。最初の書き込みで初めて打つ**（`sync.ts` の `ensureRoadmap`）。

匿名ユーザーもMAUとして課金対象になるので、共有ページを見ただけの人にまで発行すると
コストと `auth.users` が無駄に膨らむ。「使い始めるまでの摩擦をゼロにする」（5章）は
これで保てている。

なお `signInAnonymously()` で作られるのは **`anon` ロールではなく `authenticated`**。
`anon` は「JWTを持たない」という意味で、匿名認証とは別物。GRANTを読むときに混同しないこと。

### ★の2箇所は理由を読まずに触らないこと

- **`lib/dnd/roadmap-sensor.ts`** — センサーを1つにまとめて押された位置で発火条件を
  切り替える実装。2つ登録すると後勝ちで上書きされて無言で壊れる（9章）。
  握りの `touch-action: none` とカード本体の `pan-y` はセットで意味を持つので、
  `StopRow.tsx` の該当クラスも一緒に読むこと
- **`lib/books/source.ts`** — 書誌データの唯一の入口。7章「取得経路を1箇所に抽象化」の実体。
  有料化のタイミングで取得元を差し替えるとき、書き換えるのはここと同階層のクライアントだけ

### 楽天APIの入れ方

**3つとも必要**。どれかが欠けるとモックのままか、400 / 403 になる。

```
RAKUTEN_APPLICATION_ID   UUID形式
RAKUTEN_ACCESS_KEY       pk_ で始まる
RAKUTEN_ORIGIN           許可リストに登録したドメイン（サーバーから叩くので必須）
```

1. https://webservice.rakuten.co.jp/ でアプリを登録。
   **応募タイプは「ウェブアプリケーション」**（7章）
2. `.env.local` と Vercel に上の3つを入れる
3. Vercel は Production / Preview で値を出し分ける。
   プレビューのドメインは毎回変わるので、許可リストに入れられない場合は
   プレビューではモックのままになる

### Supabase で踏んだ落とし穴（2つとも自動公開を切った副作用）

**1. `gen_random_bytes()` は public から呼べない。** pgcrypto の関数で、Supabase では
拡張が `extensions` スキーマに入るため。`share_slug` の既定値に使っていて `db push` が
落ちた。`gen_random_uuid()` は Postgres 13 以降の**組み込み**なので拡張が要らない。
既定値のためだけに拡張へ依存しない。

**2. `service_role` にも権限は自動では付かない。**「Automatically expose new tables」を
切ると、`anon` / `authenticated` だけでなく `service_role` も対象外になる。
`books` への書き込みが `permission denied` で落ちた（`0004_service_role_grants.sql`）。

→ **`service_role` には `books` だけを渡している。** 他のテーブルには意図的に渡さない。
service_role は RLS を迂回する鍵なので、これでロードマップ本体を触ると
「アクセス制御はRLSに集約する」（6章）が崩れる。`admin.ts` のコメントは破れるが、
DBの権限なら破れない。サーバー側から別のテーブルを触りたくなったら、
そのとき「なぜRLSでは足りないのか」を確かめてから足すこと。

### Supabase の入れ方

楽天と同じで、**環境変数が入っていなければモックで動く**。入れた時点で切り替わる。

1. プロジェクトを2つ作る（preview / 本番）。ブランチ運用と1対1で対応させる
2. Data API まわりのトグルは **ON / OFF / ON**
   （Data API を有効・新規テーブルの自動公開を無効・自動RLSを有効）。
   自動公開を切っているぶんの GRANT は `0001_init.sql` に書いてある
3. 匿名サインインを有効にする。CAPTCHAも入れておく（誰でも無制限にユーザーを作れるため）
4. `supabase/migrations/` を **preview → 本番の順**で流す
   （CLIなら `supabase link --project-ref <ref>` → `supabase db push`。
   `db dump` は Docker が要るが、`db push` と `gen types --linked` は不要）
5. `.env.local` に URL・publishable key・secret key を入れる

### まだ入っていないもの

| 場所 | 現状 | 次にやること |
|---|---|---|
| Supabase | preview に接続・検証済み（0006まで適用） | 本番プロジェクトを作って同じSQLを流す |
| タグ | **画面から外した。** テーブルと型は残してある | 必要になったら補完UIと一緒に戻す |
| 保存状態の表示・取り消し | 未実装 | 13章の未確定事項と同じ |
| 作成者名 | 常に匿名 | ログインを入れてから（13章） |
| 出版年 | 検索結果には出るが保存されない | `books` にカラムが無い（6章のとおり） |

### プロトタイプから変えたところ

- **Google Fonts 3種をやめてシステムフォントにした。** 日本語Webフォントは初回表示を
  遅らせやすい（10章）。見出しだけ serif のシステムスタックを当てている
- **`Enter` でカードを開けるようにした。** プロトタイプは dnd-kit のリスナーを後から
  展開していたので、キーボードの `Enter` が握り潰されていた
- **共有URLを `/r/[slug]` の実ページにした。** プロトタイプは画面切り替えの模擬

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

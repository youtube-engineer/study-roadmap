-- ============================================================================
-- roadmaps.title の既定値を空にする
--
-- 作った直後に名前が入っているより、空で「無題のルート」が薄く出る方がよい。
-- 打ち始めるときに既に入っている文字を消す手間が要らない。
--
-- 実際にはアプリが常に title を明示して insert するのでこの既定値は使われないが、
-- コードとDBで意図が食い違っていると、後から読んだときにどちらが正か分からなくなる。
--
-- 表示側で空を「無題のルート」に補う（src/lib/roadmaps/title.ts）。
-- ============================================================================

alter table public.roadmaps alter column title set default '';

-- ゴール（何を目指すのか）を持たせる。
--
-- 当初はタイトルが目標を表しているとみなして終点に文言を置かなかったが、
-- タイトルは「英語」のような分野名になりやすく、「英検2級に受かる」のような
-- 目標とは別物だった。見ながら進むためのものなので本人に書かせる。
--
-- 既存の行には空文字が入る。空なら画面では GOAL とだけ出す。
alter table public.roadmaps
  add column if not exists goal text not null default '';
